<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>MeltdownMonitor</string>
    <key>CFBundleIdentifier</key>
    <string>com.matthewedmondson.meltdownmonitor</string>
    <key>CFBundleName</key>
    <string>MeltdownMonitor</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>MinimumOSVersion</key>
    <string>17.0</string>
    <key>UIDeviceFamily</key>
    <array>
        <integer>1</integer>
    </array>
    <!-- Modern launch screen: an empty UILaunchScreen guarantees full-screen,
         native-resolution layout on every device with no storyboard file. The
         old UILaunchStoryboardName pointed at a "LaunchScreen" that doesn't
         exist in the bundle, which would letterbox the app at a legacy size. -->
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>MeltdownMonitor connects to your Polar heart rate sensor over Bluetooth to read R-R intervals.</string>
    <key>NSBluetoothPeripheralUsageDescription</key>
    <string>MeltdownMonitor connects to your Polar heart rate sensor over Bluetooth to read R-R intervals.</string>

    <key>NSHealthShareUsageDescription</key>
    <string>MeltdownMonitor reads recent heart rate samples to warm-start your personal baseline so you don't have to wait for calibration on first launch.</string>
    <key>NSHealthUpdateUsageDescription</key>
    <string>MeltdownMonitor records heart rate samples and dysregulation episodes to Apple Health so you own your data and can share it with a clinician.</string>

    <key>UIBackgroundModes</key>
    <array>
        <string>bluetooth-central</string>
        <string>audio</string>
        <string>processing</string>
    </array>

    <key>NSSupportsLiveActivities</key>
    <true/>

    <!-- Designates the app icon set in Assets.xcassets so actool compiles it
         into the bundle. Without a marketing icon, App Store Connect rejects
         the upload (ITMS-90022). -->
    <key>XSAppIconAssets</key>
    <string>Assets.xcassets/AppIcon.appiconset</string>

    <!-- The app uses only OS-standard / exempt cryptography (HTTPS, SQLite — no
         custom or non-exempt algorithms). Declaring this lets each TestFlight
         build skip the manual export-compliance question that otherwise blocks
         it from reaching testers. -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
